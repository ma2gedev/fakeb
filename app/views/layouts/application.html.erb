<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Fakeb" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Fakeb</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav pull-right">
            <% if current_user %>
              <li><%= link_to "マイブックマーク", :controller => :bookmarks, :action => :show, :name => current_user.name  %></li>
              <li><%= link_to "追加", :controller => :bookmarks, :action => :new, :name => current_user.name  %></li>
              <li><%= link_to "ログアウト", :session, :method => :delete, :confirm => "ログアウトしますか？" %></li>
            <% else %>
              <li><%= link_to "ログイン", :new_session %></li>
              <li><%= link_to "新規アカウント作成", :new_account %></li>
            <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span12">
          <div id="flash">
            <% if flash[:notice].present? %>
              <div class="alert alert-info">
                <p class="flash_notice"><%= flash[:notice] %></p>
              </div>
            <% end %>
            <% if flash[:alert].present? %>
              <div class="alert alert-error">
                <p class="flash_alert"><%= flash[:alert] %></p>
              </div>
            <% end %>
          </div>

          <%= yield %>
        </div>
      </div><!--/row-->

      <hr>

      <footer>
        <p>Copyright&copy; 2012 <a target="_blank" href="https://twitter.com/ma2ge">ma2ge</a> All Rights Reserved.</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
